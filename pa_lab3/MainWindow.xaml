<Window x:Class="pa_lab3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:pa_lab3"
        mc:Ignorable="d"
        Title="Chomp Game (pa_lab3)" Height="550" Width="800"
        Background="#FFF5E6">
    
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="Складність:" VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0"/>
            <ComboBox SelectedItem="{Binding SelectedDifficulty}" Width="120" FontSize="14" VerticalContentAlignment="Center">
                <local:Difficulty>Easy</local:Difficulty>
                <local:Difficulty>Medium</local:Difficulty>
                <local:Difficulty>Hard</local:Difficulty>
            </ComboBox>
            <Button Content="Нова гра" Command="{Binding NewGameCommand}" Margin="20,0,0,0" Padding="15,5" FontSize="14" Background="#DDDDDD"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="2" Background="White" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ItemsControl ItemsSource="{Binding Cells}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="{Binding Cols}" Rows="{Binding Rows}"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding ClickCommand}" 
                                CommandParameter="{Binding}"
                                Margin="1" Width="50" Height="50"
                                Background="{Binding BackgroundColor}"
                                BorderBrush="Gray"
                                >
                            
                            <TextBlock Text="{Binding Symbol}" 
                                       Foreground="{Binding ForegroundColor}" 
                                       FontSize="24" 
                                       FontWeight="Bold" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"/>
                            
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="3"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <StatusBar Grid.Row="2" FontSize="14" Background="#EEE">
            <TextBlock Text="{Binding StatusMessage}" FontWeight="SemiBold"/>
        </StatusBar>
    </Grid>
</Window>